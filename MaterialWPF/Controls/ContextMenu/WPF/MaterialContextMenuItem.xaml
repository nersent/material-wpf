<UserControl x:Class="MaterialWPF.MaterialContextMenuItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MaterialWPF"
             mc:Ignorable="d" 
             d:DesignWidth="250"
             Width="Auto"
             Height="{Binding ActualHeight, ElementName=text}"
             >
    <Grid x:Name="MainGrid" ClipToBounds="True" Background="Red">
        <TextBlock x:Name="t" FontSize="16" Width="Auto" Margin="24,0,0,0" Padding="0,8,32,8" TextWrapping="WrapWithOverflow" HorizontalAlignment="Stretch" VerticalAlignment="Top">
           XDD
        </TextBlock>
    </Grid>
</UserControl>
